# Variables de entorno para la aplicación MANPOWERS
# Copia este archivo como .env y configura los valores apropiados

# Token de API para autenticación con el backend
# IMPORTANTE: Mantén este token seguro y no lo compartas
VITE_API_TOKEN=tu_token_aqui

# URL del backend en producción (opcional, tiene valor por defecto)
VITE_BACKEND_URL=https://manpowers.es/backend